---
id: Operators
aliases:
  - operators
tags:
  - javascript
  - p
---
extension from [[JavascriptOperators|javascriptoperators]]

operators operate tasks related to variables

`=` is assign
`+` is add (string + num = string)
`-` is remove
`*` and `/` are self explanitory
`**` is exponentation
`%` is modulus
`++` is increment
`--` is decreament
`>` and `<` compares values

expansion: [[ComparisionOperators|comparision operators]]
expansion: [[AssignmentOperators|assignment operators]]
expansion: [[LogicalOperators|logical operators]]
